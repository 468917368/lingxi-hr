# 开发环境配置
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848

logging:
  level:
    com.lingxi.gateway: DEBUG
    org.springframework.cloud.gateway: DEBUG
